import pyttsx3
engine = pyttsx3.init()
engine.say("authorisation factors")
engine.runAndWait()

# this library/module (pyttsx3) is ued for python text to sepach.
# the whole code which is written is used to intiate the speach.    
# VERY IMPORTANT: To use external libraries, we first pip install then import. built-in libraries only need import.